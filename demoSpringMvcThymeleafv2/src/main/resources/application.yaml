spring:
  application:
    name: demoSpringMvcThymeleaf
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      data-locations: classpath*:db/data.sql
